#include<iostream>
using namespace std;
int main() {
	int n;
	cin >> n;
	int[] apples = int[n];
	int removeCount = (n - 1) / 3;
	int[] newArray = int[newCount];
	while (n > 0) {
		for (int i = 0; i < n; i++) {
			if (i == 0) {
				continue;
			}
		}
		n = n - removeCount;
	}
	cout <<
	     return 0;
}